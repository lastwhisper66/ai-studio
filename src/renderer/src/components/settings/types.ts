export interface SettingsFormState {
  temperature: string
  temperatureEnabled: string
  topP: string
  topPEnabled: string
  contextCount: string
  maxTokens: string
  maxTokensEnabled: string
  streaming: string
  systemPrompt: string
}
