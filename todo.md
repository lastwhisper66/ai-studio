- [ ] 点击聊天框的清空消息后,输入框无法使用
- [ ] 翻译页面点击最右侧的清除历史没有反应,什么也没有发生
- [ ] 添加新助手时,因为必须要选择一个模型,所以创建新助手后这个助手有自己的默认模型,并且新建话题时也可以自动复用这个模型.但是对于默认助手,应用第一次启动时没有默认模型,在聊天页面最上方的默认助手右侧选择模型后,由于只针对当前话题,所以在默认助手下新建话题时,新话题还是没有默认模型,需要用户每次都要选择模型,比较麻烦,而程序启动时默认助手就已经存在,用户不会考虑到在助手列表右键默认助手,为默认助手添加一个默认模型,所以导致使用上非常不便,帮我考虑一下这个现象,看看如何优化会比较好
